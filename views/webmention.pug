extends _public.pug

block content
  h1= title
  p If the platform you are using doesn’t already support Webmentions, then you can use this form to manually send one for content you link to on my website.
  form
    .form-field
      label(for="#source") Source
      p.hint#source-hint The URL for your content.
      input#source(name="source" type="url" aria-describedby="target-hint")
    .form-field
      label(for="#target") Target
      p.hint#target-hint The URL for my content.
      input#target(name="target" type="url" aria-describedby="target-hint")
    button Send Webmention
